[package]
name = "macheim"
version = "1.3.2"
description = "Valheim Mod Manager for macOS"
authors = ["iyeonsang"]
license = "MIT"
repository = "https://github.com/LovelessCodes/macheim"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[lints.clippy]
# install/emit helpers pass many explicit arguments; splitting them into structs
# would be a larger refactor than the current call sites justify.
too_many_arguments = "allow"

[lib]
# The `_lib` suffix may seem redundant but it is necessary
# to make the lib name unique and wouldn't conflict with the bin name.
# This seems to be only an issue on Windows, see https://github.com/rust-lang/cargo/issues/8519
name = "macheim_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
tauri-plugin-opener = "2"
tauri-plugin-dialog = "2"
tauri-plugin-clipboard-manager = "2"
tauri-plugin-shell = "2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
serde_yaml_ng = "0.10"
base64 = "0.22"
reqwest = { version = "0.13", default-features = false, features = [
  "json",
  "stream",
  "rustls",
  "http2",
  "system-proxy",
] }
tokio = { version = "1", features = ["sync", "time"] }
zip = { version = "8", default-features = false, features = ["deflate-flate2-zlib-rs"] }
dirs = "7"
thiserror = "2"
chrono = { version = "0.4", default-features = false, features = ["clock", "std", "serde"] }
semver = { version = "1", features = ["serde"] }
plist = "1"
regex = "1"
sha2 = "0.11"
hex = "0.4"
tempfile = "3"
pelite = { version = "0.10", default-features = false, features = ["std"] }
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter"] }
futures-util = "0.3"
tauri-plugin-updater = "2.11.0"
tauri-plugin-process = "2.3.1"

[dev-dependencies]
wiremock = "0.6"
